fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
python-frontmatter>=1.1
markdown-it-py>=3.0
watchdog>=4.0
PyYAML>=6.0
python-multipart>=0.0.9
fpdf2>=2.7
gherkin-official>=29
httpx>=0.27

# Só para LER um certificado e dizer quem o assinou no diagnostico (change
# 0188). Em rede que re-assina TLS, o nome do emissor e a resposta para "qual
# .pem eu preciso?" — e uma resposta que so aparece quando a biblioteca esta
# instalada nao serve para quem esta travado.
cryptography>=42
keyring>=25
argon2-cffi>=23.1

# Servidor MCP (change 0146): expoe o workspace para um agente. Versao 2.x —
# a API mudou de FastMCP para MCPServer na 2.0, e o codigo usa a nova.
mcp>=2.0
