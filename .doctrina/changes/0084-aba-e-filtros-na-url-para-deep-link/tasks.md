# Tasks — Change 0084-aba-e-filtros-na-url-para-deep-link

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [ ] `App.tsx`: parse/serialize do hash (aba + query), listener de hashchange.
- [ ] Serializar filtros: dashboard(squad), TcRepository, Memory, Automation(tab).
- [ ] `onNavigate` grava hash.
- [ ] `npm run build` + revisão visual (incl. back/forward).

## Closing steps

- [ ] Apply the change: merge each delta into the corresponding spec.
- [ ] Archive the change folder to `.doctrina/changes/archive/2026-07-19-0084-aba-e-filtros-na-url-para-deep-link/`.
- [ ] Update `.doctrina/index.json` with new or modified artifacts.
