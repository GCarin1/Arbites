# Tasks — Change 0062-design-system-orientacao-e-navegacao-assistida

- [ ] Componente `Breadcrumbs` + aplicar em: repositório de CTs (pastas),
      detalhe de execução, editores de CT/requisito.
- [ ] Header de contexto: título da seção sempre visível; reforçar destaque
      da rota atual no menu.
- [ ] `CommandPalette` (Ctrl+K): busca via `GET /search` existente, navega
      pelo `navigateTo`, + ações rápidas (novo CT/execução/requisito).
- [ ] Estender navegação por menção/refs clicáveis aos cards/tabelas que
      ainda não têm (Defects, Executions, Dashboard).
- [ ] Telas grandes: `.content-narrow` (max-width de leitura) onde couber,
      painéis colapsáveis, ritmo vertical.
- [ ] `npm run build` limpo; revisão visual documentada.
- [ ] Atualizar spec design-system: critério #4 → verified; Implementation
      → implemented/verified (última slice) e Status → active.

## Closing steps

- [ ] Apply the change: merge each delta into the corresponding spec.
- [ ] Archive the change folder to `.doctrina/changes/archive/2026-07-13-0062-design-system-orientacao-e-navegacao-assistida/`.
- [ ] Update `.doctrina/index.json` with new or modified artifacts.
