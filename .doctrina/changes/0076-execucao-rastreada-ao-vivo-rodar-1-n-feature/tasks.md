# Tasks — Change 0076-execucao-rastreada-ao-vivo-rodar-1-n-feature

- [ ] Backend: features: list em /runs/local (posicionais multiplos) + CTs por tag+nome.
- [ ] Backend: name_map no behave_json/collect (resultado-CT por nome).
- [ ] Backend: parser incremental do plain stream (EN/PT) com persistencia por cenario.
- [ ] Backend: GET /runs/active + testes (multi-feature, live parcial, reconciliacao).
- [ ] Frontend: modal pos-disparo ver-andamento (onNavigate), toast done, multi-select de features.
- [ ] Frontend: dot pulsante no item Automacao (App consulta /runs/active no refresh).
- [ ] Build + smoke real com behave.
- [ ] Specs local-automation + executions: EARS + criterios; bump minor.

## Closing steps

- [ ] Apply the change: merge each delta into the corresponding spec.
- [ ] Archive the change folder to `.doctrina/changes/archive/`.
- [ ] Update `.doctrina/index.json` with new or modified artifacts.
