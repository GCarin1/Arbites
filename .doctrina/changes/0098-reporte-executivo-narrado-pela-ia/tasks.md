# Tasks — Change 0098-reporte-executivo-narrado-pela-ia

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [ ] Backend executive-summary (métricas+defeitos no prompt; MockTransport nos testes).
- [ ] UI Dashboard: gerar → preview editável → incluir no export.
- [ ] Export PDF/MD com a seção inicial quando aceita.
- [ ] Testes + `npm run build`.

## Closing steps

- [ ] Apply the change: merge each delta into the corresponding spec.
- [ ] Archive the change folder to `.doctrina/changes/archive/2026-07-19-0098-reporte-executivo-narrado-pela-ia/`.
- [ ] Update `.doctrina/index.json` with new or modified artifacts.
