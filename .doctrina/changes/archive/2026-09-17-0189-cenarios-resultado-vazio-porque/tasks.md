# Tasks — Change 0189-cenarios-resultado-vazio-porque

- [x] `e_relatorio_cucumber()` com corte barato antes de desserializar.
- [x] `extrair_cenarios()` e `_por_convencao()` pela forma; nome sai da tabela.
- [x] `reprocessar()` + `POST /ci/reprocess`.
- [x] Botão "Reprocessar do disco" na aba Configuração.
- [x] `backend/tests/test_cenarios_por_forma.py`.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-17-0189-cenarios-resultado-vazio-porque/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
