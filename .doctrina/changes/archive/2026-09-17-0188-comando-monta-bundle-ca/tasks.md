# Tasks — Change 0188-comando-monta-bundle-ca

- [x] `bundle_ca.py`: montar a partir do armazenamento do sistema + `certifi`.
- [x] Subcomando `bundle-ca --saida` na CLI.
- [x] `diagnostico.quem_assinou()` e o encaixe na seção de rede.
- [x] `tls.explicacao()` aponta o comando em vez de sugerir uma caçada.
- [x] `cryptography` em `requirements.txt`.
- [x] README: seção de rede corporativa.
- [x] `backend/tests/test_bundle_ca_do_sistema.py`.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-17-0188-comando-monta-bundle-ca/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
