# Tasks — Change 0191-taxa-sucesso-falhas-contando

- [x] `e_conclusiva()` e `taxa_de_sucesso()` — a conta num lugar só.
- [x] `painel()`, `_recorte()` e `distribuicao()` usando a conta nova.
- [x] Card da taxa com o denominador; pizza com rodapé do que ficou fora.
- [x] `backend/tests/test_execucao_conclusiva.py`.
- [x] README: a regra do veredito.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-17-0191-taxa-sucesso-falhas-contando/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
