# Tasks — Change 0116-modo-guiado-busca-titulo

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [x] Trocar as N chamadas de `api.testcase(id)` do modo guiado por uma leitura de `api.testcases()`, como o Kanban já faz.
- [x] Cobrir com teste que a lista de casos entrega título de todos os casos do ciclo numa leitura só.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-13-0116-modo-guiado-busca-titulo/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
