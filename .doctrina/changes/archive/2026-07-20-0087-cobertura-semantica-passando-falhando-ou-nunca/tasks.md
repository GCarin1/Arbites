# Tasks — Change 0087-cobertura-semantica-passando-falhando-ou-nunca

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [x] `metrics.traceability`: incluir last_result por CT + agregado por story.
- [x] `Requirements.tsx`: badge de 4 estados + filtro por estado (substitui o binário da 0068).
- [x] Testes em `test_metrics.py` (passing/failing/untested).
- [x] `npm run build` + revisão visual.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-07-19-0087-cobertura-semantica-passando-falhando-ou-nunca/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
