# Delta — design-system (change 0084)

**Operation:** MODIFIED

Delta prose (merge manual na spec ao implementar, padrão do repo).

## MODIFIED — Requirements (EARS)

- The system shall refletir a aba ativa e os filtros de alto valor
  (dashboard squad, filtros do repositório de CTs, período da Memória, aba
  interna da Automação) num hash de URL restaurável — deep-link
  compartilhável, back/forward do navegador funcionais, sem lib de router.
  [unverified]

## Acceptance criteria (append — numeração final no merge)

- [unverified] URL com hash restaura aba e filtros; back/forward
  navegam; onNavigate produz link compartilhável — verified by build +
  revisão visual.
