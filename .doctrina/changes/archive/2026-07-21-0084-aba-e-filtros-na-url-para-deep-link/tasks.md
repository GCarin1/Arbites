# Tasks — Change 0084-aba-e-filtros-na-url-para-deep-link

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [x] `App.tsx`: parse/serialize do hash (aba + query), listener de hashchange.
- [x] Serializar filtros: dashboard(squad), TcRepository, Memory, Automation(tab).
- [x] `onNavigate` grava hash.
- [x] `npm run build` + revisão visual (incl. back/forward).

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-07-19-0084-aba-e-filtros-na-url-para-deep-link/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
