# Tasks — Change 0197-area-diagnostico-fim-cenario

- [x] `ler_cucumber()` guarda a mensagem do passo que falhou.
- [x] Coluna `error` em `ci_scenarios` e sua migração.
- [x] `molde_do_erro()` e as três consultas.
- [x] `GET /ci/diagnostic` e a seção "Onde doer" no fim.
- [x] `backend/tests/test_diagnostico_observabilidade.py`.
- [x] Verificação em navegador com dois repositórios.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-17-0197-area-diagnostico-fim-cenario/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
