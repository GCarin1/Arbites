# Delta — requirements (change 0092)

**Operation:** MODIFIED

Delta prose (merge manual na spec ao implementar, padrão do repo).

## MODIFIED — Requirements (EARS)

- The system shall expor por story a contagem de critérios EARS cobertos
  (critério com ≥1 CT vinculado via `criteria`) na matriz e na aba
  Requisitos. [unverified]

## Acceptance criteria (append — numeração final no merge)

- [unverified] Contagem X/Y critérios cobertos por story — verified by
  `backend/tests/test_metrics.py`.
