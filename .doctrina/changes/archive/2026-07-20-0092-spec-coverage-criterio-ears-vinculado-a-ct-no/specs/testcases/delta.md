# Delta — testcases (change 0092)

**Operation:** MODIFIED

Delta prose (merge manual na spec ao implementar, padrão do repo).

## MODIFIED — Requirements (EARS)

- The system shall aceitar `criteria: [EARS-n, ...]` no frontmatter do
  CT (indexado), com picker na UI restrito aos critérios da story
  vinculada. [unverified]

## Acceptance criteria (append — numeração final no merge)

- [unverified] `criteria` indexado e editável pela UI — verified by
  `backend/tests/test_testcases.py`.
