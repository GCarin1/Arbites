# Tasks — Change 0040

- [x] `StepBar`: barra segmentada por passo (cor por status) no card.
- [x] `ExecStackBar`: barra empilhada por status na execução (todas as colunas).
- [x] CSS `.stepbar/.stepbar-seg/.seg-*` e `.exec-stack/.exec-seg.col-*`.
- [x] Remover a barra de fill único antiga (`kanban-card-progress-fill`, `exec-progress-fill`).
- [x] Build frontend verde.

## Closing steps

- [x] Apply: merge delta na spec executions.
- [x] Archive.
- [x] Update index.json.
