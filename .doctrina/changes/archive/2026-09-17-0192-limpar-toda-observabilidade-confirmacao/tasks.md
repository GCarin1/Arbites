# Tasks — Change 0192-limpar-toda-observabilidade-confirmacao

- [x] `previa_total()` e `limpar_tudo()` em `ci_retencao`.
- [x] `GET /ci/purge/preview` e `POST /ci/purge` (admin).
- [x] Cartão e `ConfirmModal` na aba Configuração.
- [x] `backend/tests/test_limpar_observabilidade.py`.
- [x] Verificação em navegador (desktop e telefone).

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-17-0192-limpar-toda-observabilidade-confirmacao/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
