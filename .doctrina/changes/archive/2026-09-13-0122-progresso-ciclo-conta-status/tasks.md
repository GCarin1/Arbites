# Tasks — Change 0122-progresso-ciclo-conta-status

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [x] `progress` do ciclo conta por `column || status`, como o quadro.
- [x] Cabeçalho do ciclo exibe a contagem que veio do servidor em vez de recalcular.
- [x] `backend/tests/test_execution_cycle.py`: caso arrastado é contado na coluna em que está.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-13-0122-progresso-ciclo-conta-status/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
