# Tasks — Change 0036

- [x] `looks_like_gherkin` / `parse_gherkin` / `gherkin_body` / `gherkin_folder` em ai.py.
- [x] Endpoint `/import/ai`: branch determinístico antes da IA (não exige provider).
- [x] Teste verbatim end-to-end (Feature, 'que', dois And preservados) + suíte verde.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder.
- [x] Update `.doctrina/index.json`.
