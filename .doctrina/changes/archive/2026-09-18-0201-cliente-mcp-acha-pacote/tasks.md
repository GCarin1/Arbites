# Tasks — Change 0201-cliente-mcp-acha-pacote

- [x] Reproduzir com um cliente MCP de verdade, antes de consertar.
- [x] `backend/pyproject.toml` — o projeto vira instalável.
- [x] README: `cwd` no `mcp.json` e a alternativa da instalação.
- [x] `secao_mcp()` no diagnóstico, aferindo de fora do projeto.
- [x] `--url` e `--token` no diagnóstico do MCP.
- [x] As duas mensagens apontando a mesma aba.
- [x] Testes.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-18-0201-cliente-mcp-acha-pacote/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
