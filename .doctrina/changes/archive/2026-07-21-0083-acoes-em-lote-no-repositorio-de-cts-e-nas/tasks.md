# Tasks — Change 0083-acoes-em-lote-no-repositorio-de-cts-e-nas

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [x] `TcRepository.tsx`: modo seleção + barra de ações (status/mover/excluir).
- [x] Lote no cliente com resumo X ok / Y falhas (toast) e ConfirmModal.
- [x] `Executions.tsx`: seleção múltipla + excluir em lote.
- [x] `npm run build` + revisão visual.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-07-19-0083-acoes-em-lote-no-repositorio-de-cts-e-nas/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
