# Tasks — Change 0041 (chore)

- [x] `Autocomplete.tsx`: `useSuggestions` aceita `kinds`; novo `SingleRefInput` (referência única).
- [x] `Executions.tsx`: vincular defeito vira `SingleRefInput` (kinds=defect); remove select + fetch de defeitos.
- [x] `Executions.tsx`: comentário do resultado vira `MentionTextarea` (@ referencia card); read-only quando fechado.
- [x] `TestCaseEditor.tsx`: campo Story vira `SingleRefInput` (kinds=requirement).
- [x] Build verde; `/search?kinds=` verificado com servidor real (filtra por tipo).

## Closing steps

- [x] Apply (chore, zero deltas).
- [x] Archive.
- [x] Update index.json.
