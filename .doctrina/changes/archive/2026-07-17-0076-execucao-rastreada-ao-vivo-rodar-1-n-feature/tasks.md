# Tasks — Change 0076-execucao-rastreada-ao-vivo-rodar-1-n-feature

- [x] Backend: features: list em /runs/local (posicionais multiplos) + CTs por tag+nome.
- [x] Backend: name_map no behave_json/collect (resultado-CT por nome).
- [x] Backend: parser incremental do plain stream (EN/PT) com persistencia por cenario.
- [x] Backend: GET /runs/active + testes (multi-feature, live parcial, reconciliacao).
- [x] Frontend: modal pos-disparo ver-andamento (onNavigate), toast done, multi-select de features.
- [x] Frontend: dot pulsante no item Automacao (App consulta /runs/active no refresh).
- [x] Build + smoke real com behave.
- [x] Specs local-automation + executions: EARS + criterios; bump minor.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
