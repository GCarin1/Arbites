# Delta — home (change 0080)

**Operation:** MODIFIED

Delta prose (merge manual na spec ao implementar, padrão do repo).

## MODIFIED — Requirements (EARS)

- (capability nova — o conteúdo completo já está em
  `.doctrina/specs/home/spec.md`, escrito no scaffold desta change; este
  delta registra a origem.) [unverified]

## Acceptance criteria (append — numeração final no merge)

- [unverified] Critérios 1-2 da spec `home` — verified by build +
  revisão visual (`frontend/src/components/Home.tsx`).
