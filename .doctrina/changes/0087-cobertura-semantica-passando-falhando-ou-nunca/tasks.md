# Tasks — Change 0087-cobertura-semantica-passando-falhando-ou-nunca

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [ ] `metrics.traceability`: incluir last_result por CT + agregado por story.
- [ ] `Requirements.tsx`: badge de 4 estados + filtro por estado (substitui o binário da 0068).
- [ ] Testes em `test_metrics.py` (passing/failing/untested).
- [ ] `npm run build` + revisão visual.

## Closing steps

- [ ] Apply the change: merge each delta into the corresponding spec.
- [ ] Archive the change folder to `.doctrina/changes/archive/2026-07-19-0087-cobertura-semantica-passando-falhando-ou-nunca/`.
- [ ] Update `.doctrina/index.json` with new or modified artifacts.
